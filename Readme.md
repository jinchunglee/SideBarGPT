chrome-ai-extension/
│── manifest.json       # Chrome 擴充插件的配置
│── popup.html          # UI 前端
│── popup.js            # UI 操作邏輯
│── content.js          # 擷取網頁內容
│── background.js       # 訊息傳遞
│── server.py           # Flask 伺服器，負責處理 Ollama 請求
│── styles.css          # UI 樣式
└── icon.png            # 插件的圖示